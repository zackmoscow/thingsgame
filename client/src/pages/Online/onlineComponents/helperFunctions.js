export function turnUserInfoIntoArray() {
    let users = [];
    // for (let key in userInfo) {
    //     users.push({
    //         userName: key,
    //         ...userInfo[key]
    //     })
    // }
    return users;
}

